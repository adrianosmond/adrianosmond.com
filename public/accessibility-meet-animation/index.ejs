<p>I like to think I'm one of the good guys. I'm not always perfect, but I do try pretty hard to keep the things that I build accessible. Since I tend to build websites (which are made of HTML and CSS with a little JavaScript) rather than webapps (which are made of JavaScript, more JavaScript and a little HTML and CSS) most of the time this is painless. The odd <code>alt</code> tag here, a quick check of a <a href="http://snook.ca/technical/colour_contrast/colour.html" target="_blank">colour contrast checker</a> there, and you're good. Every once in a while, however, I come across something that can be a little trickier.</p>

<p>Tab order is something that usually takes care of itself. Even something like an accordion, which has content that can be made visible or hidden will just work, as items which have <code>display: none</code> will not be able to receive the focus. Unfortunately, <code>display: none</code> has some consequences that you might not expect when </p>